package com.epam.digital.data.platform.excerpt.model;

import org.junit.jupiter.api.Test;

/**
 * Just for success Jenkins build, which expects some non-empty test report.
 */
class NoopTest {

    @Test
    void shouldSuccess() {
        //noop
    }
}
